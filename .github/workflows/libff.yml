name: Build libffi for Android aarch64

on:
  push:
  workflow_dispatch:

jobs:
  build-libffi:
    runs-on: ubuntu-latest
    env:
      NDK_VERSION: r26b
      NDK_URL: https://dl.google.com/android/repository/android-ndk-r26b-linux.zip
      # Google官方NDK r26b Linux版本 正确SHA256校验值
      NDK_SHA256: ad73c0370f0b0a87d1671ed2fd5a9ac9acfd1eb5c43a7fbfbd330f85d19dd632
      TARGET_API: 21
      TARGET_ARCH: aarch64-linux-android

    steps:
      - name: Checkout libffi 稳定版源码
        uses: actions/checkout@v4
        with:
          repository: libffi/libffi
          ref: v3.4.6
          path: libffi
          clean: true

      - name: 安装完整构建依赖
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            autoconf \
            automake \
            libtool \
            pkg-config \
            make \
            gcc \
            g++ \
            curl \
            unzip \
            m4 \
            coreutils

      - name: 下载并安装Android NDK
        run: |
          # 带重试下载，避免网络波动失败
          curl -fSL --retry 5 --retry-delay 10 -o android-ndk.zip $NDK_URL
          
          # 完整性校验
          echo "$NDK_SHA256  android-ndk.zip" | sha256sum --check --status
          
          # 解压并配置全局环境变量
          unzip -q android-ndk.zip
          mv android-ndk-$NDK_VERSION $HOME/android-ndk
          echo "ANDROID_NDK_HOME=$HOME/android-ndk" >> $GITHUB_ENV
          echo "ANDROID_NDK_ROOT=$HOME/android-ndk" >> $GITHUB_ENV
          echo "TOOLCHAIN=$HOME/android-ndk/toolchains/llvm/prebuilt/linux-x86_64" >> $GITHUB_ENV

      - name: 彻底解决文件缺失+宏报错 预构建配置
        run: |
          cd libffi
          
          # 1. 全量清理所有旧构建缓存，彻底杜绝残留干扰
          rm -rf autom4te.cache configure aclocal.m4 ltmain.sh Makefile.in */Makefile.in fficonfig.h.in fficonfig.h config.h.in config.h
          mkdir -p m4 && rm -rf m4/*
          
          # 2. 强制复制libtool完整宏文件到项目目录
          libtoolize --force --copy --install
          
          # 3. 核心兜底：手动注入LT_SYS_SYMBOL_USCORE宏，彻底解决未定义报错
          cat > m4/ltsys_underscore.m4 << 'EOF'
          AC_DEFUN([LT_SYS_SYMBOL_USCORE],
          [AC_CACHE_CHECK([whether C symbols are prefixed with underscore],
          [lt_cv_sys_symbol_underscore],
          [AC_LINK_IFELSE([AC_LANG_PROGRAM([],
          [[extern int foo(); int main() { return foo(); }]])],
          [AC_LINK_IFELSE([AC_LANG_PROGRAM([],
          [[extern int _foo(); int main() { return _foo(); }]])],
          [lt_cv_sys_symbol_underscore=yes],
          [lt_cv_sys_symbol_underscore=no])],
          [AC_MSG_ERROR([cannot check for symbol underscore])])])
          AS_IF([test "$lt_cv_sys_symbol_underscore" = yes],
          [AC_DEFINE([SYMBOL_UNDERSCORE], 1,
          [Define to 1 if C symbols are prefixed with an underscore.])])
          ])
          EOF
          
          # 4. 【核心修复】自动执行完整autotools流程，自动补全所有缺失步骤
          # autoreconf会按标准顺序自动执行：aclocal → autoheader → automake → autoconf，不会漏掉任何环节
          autoreconf -fiv -I m4 --warnings=none
          
          # 5. 前置校验：确认必须文件已生成，提前拦截问题
          test -f fficonfig.h.in || {
            echo "ERROR: 核心文件fficonfig.h.in生成失败！"
            ls -la
            exit 1
          }
          test -f configure || {
            echo "ERROR: configure脚本生成失败！"
            exit 1
          }

      - name: 交叉编译libffi for Android aarch64
        run: |
          cd libffi
          
          # 完整Android交叉编译工具链配置
          export TARGET=$TARGET_ARCH
          export API=$TARGET_API
          export CC=$TOOLCHAIN/bin/${TARGET}${API}-clang
          export CXX=$TOOLCHAIN/bin/${TARGET}${API}-clang++
          export AR=$TOOLCHAIN/bin/llvm-ar
          export AS=$TOOLCHAIN/bin/llvm-as
          export RANLIB=$TOOLCHAIN/bin/llvm-ranlib
          export STRIP=$TOOLCHAIN/bin/llvm-strip
          export SYSROOT=$TOOLCHAIN/sysroot
          
          # 适配Android的编译配置，关闭文档构建避免额外依赖报错
          ./configure \
            --host=$TARGET \
            --prefix=$PWD/build \
            --disable-shared \
            --enable-static \
            --with-sysroot=$SYSROOT \
            --disable-docs \
            --disable-dependency-tracking
          
          # 并行编译安装+产物架构验证
          make -j$(nproc)
          make install
          
          echo "===== 构建产物验证 ====="
          ls -la build/lib/
          ls -la build/include/
          file build/lib/libffi.a | grep "aarch64"

      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: libffi-android-aarch64
          path: libffi/build
